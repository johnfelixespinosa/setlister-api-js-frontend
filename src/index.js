const app = new App()

//App flow
//index js creates new instance of App
//App once created, creates a new instance of songs
//songs creates an array of songs using the songs adapter which fetches the api data with getSongs()